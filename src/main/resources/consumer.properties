# Redpanda/Kafka Consumer Configuration

# Bootstrap servers
bootstrap.servers=127.0.0.1:19092,127.0.0.1:29092,127.0.0.1:39092

# Topic name
topic.name=test-topic

# SASL Authentication
sasl.username=superuser
sasl.password=secretpassword

# Avro Schema Registry
schema.registry.url=http://localhost:8081

# SSL/Trust Store Configuration
# Leave empty or comment out if not using SSL
ssl.truststore.path=
ssl.truststore.password=

# Security Protocol
security.protocol=SASL_SSL

# SASL Mechanism
sasl.mechanism=SCRAM-SHA-512

# Consumer Group ID
consumer.group.id=avro-consumer-group

# Consumer Configuration
auto.offset.reset=earliest
session.timeout.ms=30000
heartbeat.interval.ms=10000
request.timeout.ms=30000
max.poll.interval.ms=300000

# Prometheus Metrics Configuration
metrics.port=8083

# Consumption Mode Configuration
# Options: "all" - consume all messages from offset 0
#          "last" - consume last N messages (use consume.last.count)
#          "latest" - consume only latest/newest messages (continuous)
consume.mode=all

# Number of messages to consume when consume.mode=last
# Ignored for other modes
consume.last.count=10

